#define PI 3.1416
#define PI2 PI \
    + 1 - 1
#define _LINUX_DEFINED

#define SUM(N) (N)+1+2

#define MUL (A, B) \
        (N)+1+2

#include <stdio.h>

int main(int argc, const char * argv) 
{
    printf("PI2 is %d\n", PI2, SUM(PI2));
    return 0;
}

